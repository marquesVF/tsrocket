import { Service } from 'tsrocket'
{{#each services}}
import {{this.name}}Service from './{{this.variable}}'
{{/each}}

@Service()
export default class {{ name }}Service implements BaseService {

    {{#each services}}
    @Inject()
    private readonly {{this.variable}}Service: {{this.name}}Service
    {{/each}}

}
