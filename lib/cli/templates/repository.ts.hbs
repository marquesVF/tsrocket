import { EntityRepository, Repository } from 'typeorm'
import {{modelClass}} from '../models/{{modelName}}'

@EntityRepository({{modelClass}})
export default class {{modelClass}}Repository extends Repository<{{modelClass}}> {
    /* Add you model logic inside here */
}
